# 작업 원칙 (코딩 프로젝트 전용)

이 파일은 코딩 프로젝트 폴더에만 둔다. 홈 세션(서버 운영)에는 적용하지 않는다 —
운영 지침은 `~/claude-share/instructions/`가 담당하고, 여기는 코드를 쓰는 동안의 성향 교정만 담는다.

- 해결책을 설계하기 전에, 이미 자리 잡은 제품·라이브러리가 같은 문제를 어떻게 푸는지 먼저 조사한다.
  접근 방식을 처음부터 발명하지 말고 검증된 패턴과 관례를 채택한다.
- 하위 호환 레이어·폴백·마이그레이션을 덧붙이지 말고 쓰이지 않는 경로를 삭제한다.
  (공개 저장소에서 외부가 쓰는 API·CLI 인자를 바꿀 때만 예외 — 아래 「프로젝트 예외」에 적는다.)
- 현재 요구사항을 완전히 충족하는 가장 단순한 구현을 고른다. 추측에 근거한 추상화·설정값·간접 계층을 만들지 않는다.
- 시스템은 레이어로 키운다. 엔드투엔드로 동작하는 최소 버전에서 시작해 이미 동작하는 결과물 위에 기능을 하나씩 얹는다.
  동작하는 코드를 미완성 복잡도와 맞바꾸지 않는다.
- 컴포넌트는 모듈로 분리하고 관심사를 명확히 나눈다.
- 검증되고 유지보수되는 라이브러리가 전체 복잡도를 낮추거나 안정성을 높이면 그것을 쓴다.
  흔한 기능을 명확한 이유 없이 재구현하지 않는다.
- 직접 구현하거나 패키지를 추가하기 전에 이미 설치된 의존성부터 확인한다.
  문서와 타입을 확인하지 않은 채 "이 라이브러리엔 그 기능이 없다"고 단정하지 않는다.
- 아키텍처 결정은 장기 관점으로 한다. 지금만 넘기고 나중에 교체할 임시방편을 받아들이지 않는다.

**여기 적지 않는 것**: 디렉토리 구조·설치된 패키지 목록·코드 스타일처럼 파일을 뒤져 알아낼 수 있는 정보.
적으면 위 원칙들의 밀도만 떨어진다.

# 프로젝트 예외

- **이 저장소는 공개 OSS다(github.com/renovys/codex-run, MIT).** 위 2번 원칙(하위 호환 레이어를
  덧붙이지 말고 삭제)을 **CLI 인터페이스에는 적용하지 않는다.** 플래그 이름·기본값·종료코드는
  외부 사용자가 스크립트로 묶어 쓰는 계약이다. 바꿔야 하면 새 플래그를 더하고 기존 것은
  한동안 유지하며, 제거는 별도 작업으로 분리해 CHANGELOG 에 적는다.
  내부 구현·헬퍼 함수에는 2번 원칙을 그대로 적용한다.
- 작업 완료 전 검증: `bash -n codex-run`(셸 문법), `--help` 종료 0, 미지 옵션 종료 2.
- 개발 환경의 절대경로·내부 IP·계정명을 코드나 예시에 넣지 않는다. 공개 저장소다.
